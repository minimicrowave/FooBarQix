const NUMBER_TEXT_MAP = {
	3: 'Foo',
	5: 'Bar',
	7: 'Qix'
};

const NUMBER_LIST = Object.keys(NUMBER_TEXT_MAP).map(Number);

const EMPTY_STRING = '';

module.exports = {
	NUMBER_TEXT_MAP,
	NUMBER_LIST,
	EMPTY_STRING
};
