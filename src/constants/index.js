const NUMBERS = {
	foo: 3,
	bar: 5,
	qix: 7
};

const TEXTS = {
	foo: 'Foo',
	bar: 'Bar',
	qix: 'Qix'
};

const VARIABLES = [ 'foo', 'bar', 'qix' ];

const EMPTY_STRING = '';

module.exports = {
	NUMBERS,
	TEXTS,
    VARIABLES,
    EMPTY_STRING
};
